{
    "scripts": {
        "dev": "space dev",
        "install:app": "cd app && npm install",
        "install:api": "cd api && npm install",
        "install:all": "npm run install:app && npm run install:api"
    }
}